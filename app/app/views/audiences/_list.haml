%a{href: new_audience_path, class: (action_name == "index") ? "pure-button pure-button-primary" : "pure-button"}
  %i.fa.fa-plus
  Nueva audiencia

.cards
  - if @audiences.empty?
    .card.disabled Aquí no hay nada
  - else
    - @audiences.each do |audiencia|
      = link_to audiencia, {class: "card"} do
        = audiencia.nombre
