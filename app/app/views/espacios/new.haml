%h1
  = render partial: "common/back", locals: {path: medio_espacios_path}
  Nuevo espacio
%h3
  en medio
  = link_to @medio.nombre, @medio

= form_for [:medio, @espacio], url: medio_espacios_path, html: {class: "pure-form"} do |f|
  %p
    Dimensiones:
    = f.number_field :ancho, in: 16..1024, placeholder: "Ancho"
    x
    = f.number_field :alto, in: 16..1024, placeholder: "Alto"

  %p
    = f.check_box :multimedia
    = f.label :multimedia, "El espacio permite contenido audiovisual"

  %p
    = f.check_box :enlace
    = f.label :enlace, "El espacio permite enlazar a otro sitio web"

  %p
    = f.submit "Crear espacio", class: "pure-button pure-button-primary"
